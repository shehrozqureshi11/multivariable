# Prisma against Postgres / Supabase
#
# 1. Set DATABASE_URL in root .env (pooler or direct URI both work)
# 2. From repo root:
#      npm run db:generate
#      npm run db:push
# 3. Seed demo data:
#      npm run db:seed


